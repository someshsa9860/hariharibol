# HariHariBol

Fresh start. Three parts:

- `app/` — mobile app
- `backend/` — API
- `admin/` — admin panel

The previous implementation is archived on the `unorganized` branch (`git checkout unorganized`).
