import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - devotional theme (from design)
  static const Color primary = Color(0xFFC75A1A); // Orange-brown (design accent)
  static const Color secondary = Color(0xFF8B4513); // Saddle brown
  static const Color accent = Color(0xFFDAA520); // Goldenrod

  // Backgrounds (from design)
  static const Color backgroundLight = Color(0xFFFBF7EF); // Cream (design background)
  static const Color backgroundDark = Color(0xFF1A1A2E); // Dark (adjusted for contrast)

  // Surfaces
  static const Color surfaceLight = Color(0xFFFFFDFA); // Almost white cream
  static const Color surfaceDark = Color(0xFF2D2D3D);

  // Text (from design)
  static const Color textPrimaryLight = Color(0xFF1A1410); // Dark brown (design text)
  static const Color textSecondaryLight = Color(0xFF8B7D73); // Lighter brown
  static const Color textPrimaryDark = Color(0xFFF5F1EB); // Cream text on dark
  static const Color textSecondaryDark = Color(0xFFC9BFB5); // Lighter cream

  // Error
  static const Color error = Color(0xFFD32F2F);

  // Success
  static const Color success = Color(0xFF388E3C);

  // Warning
  static const Color warning = Color(0xFFF57C00);

  // Info
  static const Color info = Color(0xFF1976D2);
}