# App — HariHariBol Mobile

Flutter mobile app (iOS + Android).

## Rules

_Not yet defined — rules for this part are still to be shared._

Nothing should be built here until they are. When they arrive, they get written into this file before any code is written.

## Notes carried over

- The previous Flutter app is archived on the `unorganized` branch at `mobile_app/` — available as reference, not as a base to copy.
- The Android signing keystore (`key.jks`, `key.properties`) and the Firebase configs are backed up outside the repo at `../hariharibol-local-secrets/`. The keystore is irreplaceable — losing it blocks Play Store updates for the existing listing.
